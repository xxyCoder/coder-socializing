export const enum NotifyStateMap {
  unread,
  read
}

export const enum NotifyTypeMap {
  comment,
  at,
  thumb,
  collect,
  concern,
  'self-comment'
}

export const enum InteractionTypeMap {
  'comment-at',
  'thumb-collet',
  concern,
  'self-comment'
}
